# Session: 2026-09-01

## Goal
Recover VideoCorruptor, define the PlayPlayPlay adversarial MXF corpus, and execute its bounded generator foundation through Wave 3.

## Progress
- Recovered the repository and audited all five existing MXF mutations for parser-conformance, decoder-integration, and fuzz-stress suitability.
- Approved `specs/mxf-adversarial-corpus.md` and created the seven-wave implementation plan.
- Completed Wave 0: added the hosted macOS test target, checked binary arithmetic/spans and data access, schema v1 models, canonical manifest validation/path safety, and fixture-keyed seed derivation.
- Completed Wave 1: added strict BER metadata, bounded streaming KLV inspection, chunked copy and incremental SHA-256, exact mutation verification, atomic staging/publication, and the serial generator shell.
- Corrected the integration boundary so corpus source inspection reads only keys and BER headers with `FileHandle` and seeks over validated payloads instead of mapping whole files.
- Verified reproducible XcodeGen output, a successful Debug build, and 84 passing tests with zero failures.
- Created eleven atomic Wave 0–1 implementation commits and pushed `main` through documentation commit `4b7366a`.
- Completed Wave 2: added eight BER fixtures, six generic boundary truncations, three checked synthetic sources, and a canonical 14-fixture corpus golden.
- Completed Wave 3: added precise partition-pack parsing, bounded physical partition graphs, seven offset/cycle fixtures, profile-aware missing header/footer/RIP cases, and partition fixed-field truncation.
- Kept Wave 2's golden explicitly scoped to its frozen fixture IDs when Wave 3 expanded the truncation registry.
- Verified Wave 3 with a reproducible XcodeGen project, successful Debug build, and 118 passing tests across 19 suites.

## Decisions
- VideoCorruptor owns deterministic malformed-file construction; PlayPlayPlay owns parser semantics, connected by schema `com.playplayplay.mxf-adversarial-corpus` version 1 JSON.
- Parser-conformance fixtures normally contain one declared semantic defect and must pass exact-diff verification before atomic publication.
- Large-file operations remain chunked and cancellation-aware; structural inspection reads bounded headers and seeks over payloads rather than loading complete MXF files.
- Existing corpus destinations are never replaced implicitly, and source publication eligibility cannot exceed recorded source rights.
- Partition identity comes from physical key offsets and key classification, never from potentially corrupt `ThisPartition` values; missing-structure expectations require explicit OP1a or OP-Atom declarations.

## Next
- Execute Wave 4: add bounded primer/local-set inspection, bounded batch/array inspection, P0 count fixtures, and index/count semantics.
- Implement the verified index-key/local-set seam before adding missing-index behavior; it was intentionally deferred from Wave 3.
- Keep profile/source declarations conservative and return `notApplicable` when an exact single-defect fixture cannot be proven.

## Resume
- **Uncommitted work:** six documentation files contain the refreshed session log, project state, decisions, session index, task archive, and empty Current Sprint. No source-code edits are pending.
- **Unpushed commits:** Wave 2 commits `47627ec` through `3454f05` and Wave 3 commits `1452139` through `16260ce` are local on `main`; the last pushed commit is `4b7366a`.
- **Exact pickup:** run `/execute wave 4`. Begin with Task 4.1 (`MXFLocalSetInspector`/`MXFPrimerMap`) and Task 4.2 (`MXFBatchInspector`) according to the dependencies in ignored `IMPLEMENTATION_PLAN.md`.
- **Gate baseline:** XcodeGen is reproducible; Debug build succeeds; 118 tests across 19 suites pass with zero failures using `/tmp/VideoCorruptor-DerivedData` and `CODE_SIGNING_ALLOWED=NO`.
- **Intentional deferral:** missing-index behavior requires Wave 4's verified index/local-set key seam. Do not add it by guessing a generic UL or profile policy.
- **Half-done implementation:** none. Wave 3 is fully committed and gated; only documentation synchronization remains uncommitted.
