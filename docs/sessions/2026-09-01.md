# Session: 2026-09-01

## Goal
Recover VideoCorruptor and leave a clear handoff for reusing it in PlayPlayPlay's MXF work.

## Progress
- Located the public `Xpycode/Video-Corruptor` GitHub repository after the old local checkout had disappeared.
- Cloned it back to the standard local project location at `/Users/sim/ProgrammingProjects/1-macOS/VideoCorruptor`.
- Confirmed this is the intended macOS app: it deliberately creates corrupted video files for testing Video Integrity Checker and related analyzers.
- The immediate reason for recovering it is PlayPlayPlay's next implementation step: an adversarial MXF corruption/property corpus covering malformed BER, offsets, counts, cycles, missing structures, truncation, and cancellation.
- Audited all five existing MXF mutations as potential corpus generators in `research/mxf-corpus-audit-2026-09-01.md`.
- Found that BER-length shortening has the strongest direct parser-test value; footer offset and KLV-key corruption need precise variants; essence corruption belongs in decoder integration; and block-level index scrambling is fuzzing rather than a conformance fixture.
- Wrote `specs/mxf-adversarial-corpus.md`, covering a deterministic fixture service, mutation records, a versioned JSON manifest, bounded inspection, PlayPlayPlay integration, acceptance criteria, tests, and phased delivery.
- Identified the remaining policy decisions: initial MXF profiles, missing footer/RIP/index behavior, corpus storage, PlayPlayPlay error-code stability, and non-minimal BER handling.
- Reviewed and approved all remaining policy decisions; the MXF adversarial corpus spec is now approved.
- Used three parallel planning agents, the available MCP inventory, primary Apple/XcodeGen sources, both repositories, and direct code inspection to produce `IMPLEMENTATION_PLAN.md`: 33 tasks across seven dependency-gated waves.
- Mapped the consumer work to PlayPlayPlay's existing `mxf-spike` package instead of inventing a new reader; the remaining contract gap is stable IDs for `BoundedReadError` plus manifest-driven tests.

## Decisions
- Treat VideoCorruptor as the existing candidate tool to inspect and extend before building a separate corruption generator inside PlayPlayPlay.
- Keep generated fixtures and expected failure behavior deterministic so they can serve as repeatable parser tests.
- Use a UI-independent corpus-generation service in VideoCorruptor with JSON manifests as the boundary to PlayPlayPlay.
- Separate precise single-defect parser-conformance fixtures from decoder-integration and fuzz-stress files.
- Start with OP1a and OP-Atom, use profile-aware missing-structure expectations, accept valid non-minimal BER with a warning, require stable PlayPlayPlay error codes, and split small checked-in fixtures from SHA-pinned external media.

## Next
- Execute Wave 0: add the test target, checked binary arithmetic/access, schema v1 models, canonical manifest validation, and fixture-keyed seed derivation.

## Resume
- **Uncommitted tracked work:** `docs/PROJECT_STATE.md`, `docs/TASKS.md`, `docs/decisions.md`, and `docs/sessions/_index.md`, plus the new audit, approved spec, and this session log.
- **Disposable plan:** `IMPLEMENTATION_PLAN.md` contains 33 tasks across seven waves and is intentionally ignored by the repository's `*PLAN*.md` rule.
- **Exact pickup:** run `/execute` for Wave 0, beginning with Task 0.1 in `01_Project/project.yml`: add the hosted macOS `VideoCorruptorTests` target and explicit shared scheme, regenerate the tracked Xcode project, and prove test discovery.
- **Then:** implement Tasks 0.2–0.6 from the plan. Keep Wave 0 limited to tests, checked binary primitives, schema contracts, canonical validation, and fixture-keyed seeds; do not start file mutation early.
- **Verification note:** use `/tmp/VideoCorruptor-DerivedData` for `xcodebuild` because the planning-session baseline probe could not write the default DerivedData location inside the sandbox.
- **Cross-repo constraint:** do not edit PlayPlayPlay until Wave 6. Its existing ten-row `FixtureManifest` is a separate rights/feasibility contract; the adversarial corpus must use `AdversarialCorpusManifest` and its own schema identity.
- **Half-done implementation:** none. No source code, project configuration, fixture, build, or test implementation has started.
