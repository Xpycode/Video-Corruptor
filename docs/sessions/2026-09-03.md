# Session: 2026-09-03

## Goal
Checkpoint the completed MXF corpus, then correct and verify the macOS toolbar rendering.

## Progress
- Committed and pushed the completed seven-wave MXF adversarial corpus after confirming the branch
  had no incoming work or divergence.
- Compared the toolbar against Conjoyn, Penumbra, and the newer System Audio Recorder implementation.
- Adopted the compact hidden-titlebar shell and SAR-sized 28-point toolbar controls.
- Added a principal anchor so the batch action remains trailing, and suppressed macOS 26 shared
  toolbar backgrounds so the system no longer wraps the controls in oversized Liquid Glass circles.
- The user visually accepted the rebuilt toolbar. All 153 tests pass with one expected opt-in export skip.

## Decisions
- Use the newer SAR toolbar geometry as the local app-shell reference.
- On macOS 26, explicitly hide shared toolbar backgrounds; retain the existing fallback on macOS 15–25.

## Next
- Run the remaining manual checks: identical-seed reproducibility, mixed-format batch behavior, and
  blocker-conflict gating. Then begin the planned corruption-type expansion.

## Resume
- **Uncommitted work:** only the accepted toolbar implementation and this session documentation remain;
  they are committed and pushed as part of this closeout.
- **Exact pickup:** perform the three manual acceptance checks without UI automation unless the user
  explicitly requests it.
- **Verified baseline:** Debug build succeeds; all 153 tests pass with one expected export skip; the
  accepted toolbar was visually reviewed on macOS 26.
